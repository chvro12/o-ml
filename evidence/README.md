# Preuves d'execution

Deposer ici les sorties datees de `kubectl top pods`, `kubectl get all`, du
pipeline GitHub Actions et des tests de charge executes sur Minikube. Aucun
fichier de preuve Kubernetes n'est genere localement tant que Docker,
`kubectl` et Minikube ne sont pas disponibles.
